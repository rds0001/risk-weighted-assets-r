# Third-party notices

The package depends on R and CRAN packages declared in `DESCRIPTION`; each is
distributed under its own license. The source distribution does not vendor
their source code.

Regulatory publications, standards, webpages and downloaded documents are not
redistributed. `regulatory_sources()` provides official URLs and historical
checksums for provenance only.

The compressed canonical IRRBB reference path was generated by NumPy 1.26.4's
`default_rng` (PCG64) normal sampler solely as synthetic numerical test data.
No NumPy source code is included, and NumPy is not a runtime dependency. See
NumPy's own license and the provenance fields embedded with that resource.
