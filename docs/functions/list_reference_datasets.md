# `list_reference_datasets()`

Returns the inventory of calculation-ready synthetic datasets represented by
the package resources.

```r
list_reference_datasets()
```

Rows identify profile, reporting date and dataset version. Workbooks are
materialised only when requested, keeping the installed package compact.
