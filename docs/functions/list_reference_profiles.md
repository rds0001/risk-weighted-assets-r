# `list_reference_profiles()`

Returns one row per bundled synthetic bank profile, including profile ID,
version, base reporting date, default seed and description.

```r
list_reference_profiles()
```

The function is read-only and offline. Profile identifiers can be passed to
both synthetic generation functions.
