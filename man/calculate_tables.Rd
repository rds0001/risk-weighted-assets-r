% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{calculate_tables}
\alias{calculate_tables}
\title{Calculate Canonical In-Memory Tables}
\usage{
calculate_tables(tables, run_id = NULL, project_root = NULL)
}
\arguments{
\item{tables}{Named list of canonical data frames.}

\item{run_id}{Optional caller-defined run identifier.}

\item{project_root}{Optional root used to resolve local source documents.}
}
\value{
An \code{rwa_calculation_result} object.
}
\description{
Validates and calculates canonical tables without producing spreadsheet
output. A parallel fully-loaded result is calculated from the same snapshot.
}
\details{
The return includes applied and fully-loaded result tables. All
calculations use one official bitemporal snapshot. The function does not
write files and does not change the global random-number stream.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
tables <- generate_synthetic_tables(bank_profile = "KSA_BANK")
result <- calculate_tables(tables)
result$metrics[c("RWEA_KSA", "TREA")]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=generate_synthetic_tables]{generate_synthetic_tables()}}, \code{\link[=calculate_dataset]{calculate_dataset()}}
}
