% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{calculate_dataset}
\alias{calculate_dataset}
\title{Calculate a Canonical Workbook Dataset}
\usage{
calculate_dataset(dataset)
}
\arguments{
\item{dataset}{Dataset directory containing an \code{inputs} subdirectory.}
}
\value{
An \code{rwa_calculation_result} object with output paths.
}
\description{
Validates a dataset, creates an immutable deterministic run directory and
writes six structured output workbooks plus a JSON run manifest.
}
\details{
Output is written below \verb{dataset/outputs/<run-id>}. The run ID
fingerprints the input files, code and package version.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
root <- file.path(tempdir(), "rwa-calculation-example")
dataset <- generate_synthetic_dataset(root, bank_profile = "KSA_BANK")
calculate_dataset(dataset)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=validate_dataset]{validate_dataset()}}, \code{\link[=generate_synthetic_dataset]{generate_synthetic_dataset()}}
}
